export default function Button ({des, msg}) {
// function handleClick () {
// alert ('xke nn va?')
// }   

return (
        <>
        <div
      
        > 
            <button onClick={() => alert (msg)} > {des}</button>
          
        </div>
        </>
    );
}